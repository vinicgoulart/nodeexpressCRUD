NodeJS + Express CRUD
This is a NodeJS + Express CRUD API.

PATHs
To get all users, send a GET request to > /user.

To get one user, send a GET request to > /user/id.

To create one user, send a POST request to > /createuser (must use JSON).

To update one user, send a PUT request to > /user/id (must use JSON, userId not required in the body request).

To delete one user, send a DELETE request to > /user/id.

Feedback is always appreciated